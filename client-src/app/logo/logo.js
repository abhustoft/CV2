import * as React from 'react';
import * as ReactDom from 'react-dom';

var Logo = React.createClass({
  render: function() {
    return (
      <div className="logo">
        the logo
      </div>
    );
  }
});

export {Logo}
