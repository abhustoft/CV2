import * as React from 'react';
import * as ReactDom from 'react-dom';

var Photo = React.createClass({
  render: function() {
    return (
      <div className="photo">
        the Photo
      </div>
    );
  }
});

export {Photo}
